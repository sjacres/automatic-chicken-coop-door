// globals

// bearing
bearing_id_radius = 8 / 2;
bearing_od_radius = 22 / 2;
bearing_depth = 7;

// door
door_height = 342.9;
door_width = 393.7;
panel_depth = 6.35;
sides_depth = 38.1;
plexi_depth = 1.28;
door_depth = panel_depth + sides_depth + plexi_depth;
opening_height = 330.2;
opening_width = opening_height;

// tab to lock the door
tab_depth = sides_depth / 2;
tab_height = 25;
tab_width = 25;

// assembley items
pivot_center_from_edge = bearing_od_radius + tab_width / 2;
